// import { authMiddleware } from "@/middlewares/auth.middleware";
// import express from "express";
// import * as characterController from "@/controllers/character.controller";

// const router: express.Router = express.Router();

// router.use(authMiddleware);

// router.get("/", characterController.getAll)
// router.get("/:slug", characterController.getBySlug)
// router.patch("/:slug", characterController.update)
// router.post("/:id", characterController.create)
// router.delete("/:id", characterController.create)